// import { api } from '@/utils';
// import { useStateP } from '@/utils/functional-component';
import { defineStore } from 'pinia';

export const useCommonDataPinia = defineStore('commonData', () => {
  // const [] = useStateP(api.post(''));
  console.log('useCommonDataPinia');

  return {};
});
